---
const nav = [
  { href: "/", label: "主页" },
  { href: "https://infinitum.shawnxie.top/daily", label: "日报", external: true },
  { href: "https://lumina.shawnxie.top/columns", label: "专栏", external: true },
  { href: "/blog/", label: "博客" },
  { href: "/projects/", label: "项目" },
  { href: "/aff/", label: "推广" },
  { href: "/about/", label: "关于我" },
];

const currentPath = Astro.url.pathname;

const isCurrent = (href: string) => (href === "/" ? currentPath === "/" : currentPath.startsWith(href));
---

<header class="site-header">
  <div class="shell header-inner">
    <a class="brand" href="/" aria-label="肖恩聊技术首页">
      <img class="brand-mark" src="/image.png" alt="" width="36" height="36" aria-hidden="true" />
      <span>肖恩聊技术</span>
    </a>
    <nav class="site-nav" aria-label="主导航">
      {nav.map((item) => (
        <a
          href={item.href}
          aria-current={!item.external && isCurrent(item.href) ? "page" : undefined}
          target={item.external ? "_blank" : undefined}
          rel={item.external ? "noreferrer" : undefined}
        >
          {item.label}
        </a>
      ))}
    </nav>
  </div>
</header>
